import 'package:flutter/material.dart';

class SectionBrandsWidget extends StatelessWidget {
  final List<Brand> brands;
  const SectionBrandsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      color: Colors.red,
    );
  }
}